# the URL under which Quartz controller is running
quartz.controller.url=http://localhost:8080/mapmanager/quartzcontroller
# the file that stores usage information; file is bundled inside jar
usage.file=help.txt
# the folder that contains synchronization packages to be scheduled 
import.dir=<%= @content_in %>/import
# the folder that contains already scheduled synchronization packages
scheduled.dir=<%= @content_in %>/scheduled